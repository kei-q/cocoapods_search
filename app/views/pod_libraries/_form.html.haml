= form_for @pod_library do |f|
  - if @pod_library.errors.any?
    #error_explanation
      %h2= "#{pluralize(@pod_library.errors.count, "error")} prohibited this pod_library from being saved:"
      %ul
        - @pod_library.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
