= form_for @pod do |f|
  - if @pod.errors.any?
    #error_explanation
      %h2= "#{pluralize(@pod.errors.count, "error")} prohibited this pod from being saved:"
      %ul
        - @pod.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
